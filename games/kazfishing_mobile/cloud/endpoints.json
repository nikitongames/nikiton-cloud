{
  "auth": ["POST /auth/login", "POST /auth/refresh"],
  "profile": ["GET /profile", "POST /profile/update"],
  "inventory": ["GET /inventory", "POST /inventory/apply", "POST /inventory/repair"],
  "economy": ["POST /economy/grant", "POST /economy/deduct", "GET /economy/balances"],
  "market": ["POST /market/list", "POST /market/buy", "POST /market/cancel", "GET /market/search"],
  "tournaments": ["GET /tournaments/active", "POST /tournaments/submit"],
  "events": ["GET /events/active"],
  "chat": ["WS /chat/connect", "POST /chat/moderate"],
  "billing": ["POST /billing/verify", "POST /billing/redeem"],
  "anticheat": ["POST /ac/report", "GET /ac/status"],
  "notes": "Контракты клиента и бэкенда. Все монетарные операции — только серверные."
}
